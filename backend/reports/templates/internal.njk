<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>Interner Barrierefreiheitsbericht</title>
  <style>
    body { font-family: Arial, sans-serif; font-size: 14px; }
    table { border-collapse: collapse; width: 100%; margin-bottom: 20px; }
    th, td { border: 1px solid #ccc; padding: 6px; }
    th { background: #eee; }
    h1, h2 { margin-top: 30px; }
  </style>
</head>
<body>

<h1>Interner Barrierefreiheitsbericht</h1>
<p>Start-URL: {{ scan.startUrl }}</p>
<p>Datum: {{ scan.date }}</p>
<p>Gesamt-Score: {{ scan.score }}/100</p>
<p>Gecrawlte Seiten: {{ scan.pagesCrawled }}</p>
<p>Gefundene Downloads: {{ scan.downloadsFound }}</p>

<h2>Gefundene Verstöße</h2>
<table>
  <thead>
    <tr>
      <th>Seite</th>
      <th>Regel-ID</th>
      <th>Beschreibung</th>
      <th>WCAG</th>
      <th>BITV</th>
      <th>EN 301 549</th>
      <th>Rechtlicher Kontext</th>
    </tr>
  </thead>
  <tbody>
    {% for issue in issues %}
    <tr>
      <td>{{ issue.url }}</td>
      <td>{{ issue.id }}</td>
      <td>{{ issue.description }}</td>
      <td>{{ issue.wcagRefs | join(', ') }}</td>
      <td>{{ issue.bitvRefs | join(', ') }}</td>
      <td>{{ issue.en301549Refs | join(', ') }}</td>
      <td>{{ issue.legalContext }}</td>
    </tr>
    {% endfor %}
  </tbody>
</table>

<h2>Prüfung von Downloads</h2>
{% if downloads_report.length > 0 %}
  <table>
    <thead>
      <tr>
        <th>Datei</th>
        <th>Typ</th>
        <th>Check</th>
        <th>Status</th>
        <th>Details</th>
      </tr>
    </thead>
    <tbody>
      {% for file in downloads_report %}
        {% for check in file.checks %}
        <tr>
          <td>{{ file.url }}</td>
          <td>{{ file.type }}</td>
          <td>{{ check.name }}</td>
          <td>{{ check.passed ? "OK" : "Fehler" }}</td>
          <td>{{ check.details }}</td>
        </tr>
        {% endfor %}
      {% endfor %}
    </tbody>
  </table>
{% else %}
  <p>Keine Downloads oder keine Download-Prüfung aktiviert.</p>
{% endif %}

</body>
</html>
